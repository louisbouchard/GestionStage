@model IEnumerable<GestionStages.Models.MilieuStage.EntrepriseTypeMilieuStage>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Entreprises)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypesMilieuxStage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Entreprises.NomEntreprise)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TypesMilieuxStage.DescriptionTypeMilieuStage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EntrepriseTypeMilieuStageId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EntrepriseTypeMilieuStageId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EntrepriseTypeMilieuStageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
